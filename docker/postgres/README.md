# PostgreSQL container

The PostgreSQL image is configured in `docker/compose.yml`. Canonical LuxTime
schema and seed files remain under `/db`; this directory intentionally contains
no second copy of the schema.
